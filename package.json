{
  "name": "babel-plugin-decorator-test",
  "version": "1.0.0",
  "description": "Test decorators and classes.",
  "scripts": {
    "test": "npm run build && mocha --ui tdd",
    "build": "babel src --out-dir lib",
    "watch": "babel src --out-dir lib --watch"
  },
  "repository": "https://github.com/pj/babel-plugin-decorator-test",
  "license": "MIT",
  "main": "lib/index.js",
  "keywords": [
    "babel-plugin"
  ],
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-helper-plugin-test-runner": "^6.5.0",
    "babel-plugin-syntax-decorators": "^6.5.0",
    "babel-preset-es2015": "^6.6.0",
    "mocha": "^2.4.5"
  }
}
